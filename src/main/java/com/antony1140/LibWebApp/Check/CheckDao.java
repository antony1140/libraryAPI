package com.antony1140.LibWebApp.Check;
import com.antony1140.LibWebApp.DataBase.Database;
import java.sql.*;

public class CheckDao {
//    public Check getCheckbyUser() throws SQLException {
//       Connection connection = Database.getConnection();
//       String sql = "select users.user_id, users.first_name, users.last_name, books.title, books.author, books.isbn from users join checkio on users.user_id = checkio.user_id join books on books.book_id = checkio.book_id";
//    }
}
